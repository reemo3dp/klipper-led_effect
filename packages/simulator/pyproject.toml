[tool.poetry]
package-mode = false
name = "simulator"
version = "0.1.0"
description = ""
authors = ["Julian Schill <j.schill@web.de>"]
readme = "README.md"

[tool.poetry.dependencies]
wxPython = ">=4.2.5"
pyinstaller = ">6.4.0"
numpy = ">2"
klippermock = {path = "../klippermock", develop = true}

[tool.poetry.group.dev.dependencies]
black = {version = ">=26.3.1", allow-prereleases = true}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
